// Hard LOD transitions use hysteresis by default. Crossfade dithering can be re-enabled
// after it has a temporally stable implementation that does not shimmer during movement.
export const TREE_LOD_CROSSFADE_MAX_BAND_M = 0;
